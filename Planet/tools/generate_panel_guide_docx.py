from __future__ import annotations

from pathlib import Path
from zipfile import ZipFile, ZIP_DEFLATED
from xml.sax.saxutils import escape


ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "docs" / "guide_panel_placement_ru_utf8.docx"


def p(text: str, heading: int = 0) -> str:
    text = escape(text)
    if heading == 1:
        return (
            "<w:p><w:pPr><w:spacing w:before='160' w:after='80'/></w:pPr>"
            "<w:r><w:rPr><w:b/><w:sz w:val='32'/></w:rPr>"
            f"<w:t xml:space='preserve'>{text}</w:t></w:r></w:p>"
        )
    if heading == 2:
        return (
            "<w:p><w:pPr><w:spacing w:before='120' w:after='40'/></w:pPr>"
            "<w:r><w:rPr><w:b/><w:sz w:val='26'/></w:rPr>"
            f"<w:t xml:space='preserve'>{text}</w:t></w:r></w:p>"
        )
    return (
        "<w:p><w:pPr><w:spacing w:after='60'/></w:pPr>"
        "<w:r><w:rPr><w:sz w:val='22'/></w:rPr>"
        f"<w:t xml:space='preserve'>{text}</w:t></w:r></w:p>"
    )


paragraphs = [
    p("Методичка по панели размещения объектов на планете", 1),
    p("Проект: Planet5. Тема документа: как в проект была добавлена пользовательская панель выбора объектов, как загружаются ресурсы, как работает размещение объектов на сфере и как работает удаление."),

    p("1. Общая идея решения", 2),
    p("Задача была разделена на две части. Первая часть — интерфейсная панель в левом верхнем углу, через которую пользователь выбирает действие: поставить фабрику, поставить рудник или удалить уже поставленный объект. Вторая часть — игровая логика, которая получает выбранный режим и реагирует на клик по сфере: либо создаёт объект на выбранной ячейке, либо удаляет объект."),
    p("Главная идея архитектуры такая: визуальная часть панели живёт в классе HexSphereWidget, потому что это Qt-виджет, который уже отвечает за пользовательские события и отображение OpenGL-сцены. А вся логика выбора режима, постановки, проверки занятости клетки и удаления объектов живёт в InputController, потому что именно он уже обрабатывает клики мыши по планете и взаимодействует с данными сцены."),

    p("2. Какие файлы участвуют", 2),
    p("Основная визуальная часть панели находится в файлах ui/HexSphereWidget.h и ui/HexSphereWidget.cpp. Там создаются Qt-элементы интерфейса, задаётся их внешний вид, размеры, анимация и обработчики нажатий."),
    p("Логика режимов и работа со сценой находятся в controllers/InputController.h и controllers/InputController.cpp. Там описан перечислимый тип PlacementModel, хранится текущий выбранный режим, а также находятся методы setPlacementModel, placeBuildingOnCell, isCellOccupied, deleteEntityAtHit и isDeletableEntity."),
    p("Ресурсы превью для карточек лежат в resources/models. Для отображения в панели используются изображения resources/models/factory.jpg и resources/models/mine.jpg."),
    p("Сами 3D-модели в проекте уже существовали раньше и отрисовываются через meshId. Для фабрики используется строковый идентификатор factory, для рудника — mine. В рендерере по этим строкам выбирается нужная 3D-модель."),

    p("3. Как устроена панель в HexSphereWidget", 2),
    p("HexSphereWidget наследуется от QOpenGLWidget. Это значит, что он одновременно является и окном, в котором рисуется 3D-сцена, и обычным Qt-виджетом, поверх которого можно размещать интерфейсные элементы."),
    p("В конструкторе HexSphereWidget создаётся панель placementPanel_. Это объект QFrame. Он используется как контейнер для всех элементов панели. Через setStyleSheet ему задаются фон, рамка и скругление, поэтому визуально это отдельный блок поверх сцены."),
    p("Внутри placementPanel_ создаётся горизонтальный layout QHBoxLayout. Он нужен для того, чтобы слева располагалась узкая полоска раскрытия, а справа — блок карточек с действиями."),
    p("Слева создаётся placementToggleButton_. Это QToolButton. Он показывает текст Build и стрелку. При нажатии на него вызывается метод setPlacementPanelExpanded, который анимирует горизонтальное сворачивание и разворачивание панели."),
    p("Справа создаётся placementContent_. Это ещё один QFrame, внутри которого лежат сами карточки. Для него создаётся отдельный горизонтальный layout, и именно в него добавляются три карточки: Factory, Mine и Delete."),

    p("4. Какие Qt-элементы использованы и зачем", 2),
    p("QFrame используется как контейнер. Первый QFrame — это вся панель, второй QFrame — это внутренняя область карточек."),
    p("QToolButton используется как интерактивная карточка. Этот класс удобен тем, что может одновременно показывать иконку и текст, а также работать в режиме checked или unchecked. Благодаря этому карточка может визуально подсвечиваться, когда инструмент выбран."),
    p("QHBoxLayout используется для горизонтального расположения элементов: сначала для всей панели, затем для ряда карточек."),
    p("QVariantAnimation используется для плавного изменения ширины панели и ширины внутреннего блока с карточками. Это даёт эффект живого сворачивания панели по горизонтали."),
    p("QLabel используется отдельно для HUD-подсказки. Через него пользователю показываются текстовые сообщения: какой режим активирован, поставлен ли объект, можно ли удалить объект и так далее."),
    p("QPixmap и QIcon используются для загрузки изображений предпросмотра. Сначала картинка читается как QPixmap, затем заворачивается в QIcon и устанавливается в кнопку-карточку."),

    p("5. Как загружаются ресурсы карточек", 2),
    p("Внутри конструктора HexSphereWidget есть лямбда-функция configurePlacementButton. Она принимает кнопку, текст и путь к изображению."),
    p("Внутри configurePlacementButton кнопке задаются текст, режим checkable, стиль ToolButtonTextUnderIcon, размер иконки и фиксированный размер всей карточки."),
    p("Дальше Qt создаёт объект QPixmap по пути к изображению, например из файла resources/models/factory.jpg. Это и есть момент загрузки картинки предпросмотра из ресурсов проекта."),
    p("Если изображение считалось успешно, оно преобразуется в иконку и назначается кнопке-карточке. Благодаря этому на карточке появляется маленькое изображение модели."),
    p("Factory и Mine используют реальные jpg-файлы, а Delete сделана как текстовая карточка без отдельной картинки, потому что Delete — это инструмент, а не модель."),

    p("6. Как работает сворачивание и разворачивание панели", 2),
    p("Панель можно представить как два вложенных прямоугольника. Первый прямоугольник — это placementPanel_, то есть весь внешний контейнер. Второй — placementContent_, то есть область, где лежат карточки."),
    p("Когда панель раскрыта, placementPanel_ имеет ширину placementPanelExpandedWidth_, а placementContent_ имеет ширину placementContentExpandedWidth_. Эти размеры вычисляются после создания карточек."),
    p("Когда панель сворачивается, placementContent_ анимированно сжимается до нулевой ширины, а placementPanel_ — до placementPanelCollapsedWidth_. По сути остаётся только левая полоска Build."),
    p("Для этого используются две анимации QVariantAnimation. Одна анимация меняет fixedWidth у всей панели, вторая — fixedWidth у области карточек. На каждом промежуточном шаге новая ширина применяется к соответствующему виджету."),
    p("Метод setPlacementPanelExpanded принимает два параметра: expanded и animated. Если animated равно true, ширина изменяется плавно. Если false, нужное состояние выставляется сразу, например при начальной инициализации."),

    p("7. Как панель понимает, какая карточка выбрана", 2),
    p("В InputController объявлено перечисление PlacementModel. Оно описывает все инструменты панели: None, Factory, Mine и Delete."),
    p("Текущее выбранное состояние хранится в поле placementModel_. Когда пользователь нажимает карточку, из HexSphereWidget вызывается togglePlacementSelection."),
    p("Метод togglePlacementSelection сравнивает: если пользователь нажал на уже выбранный инструмент, то режим снимается и устанавливается None. Если нажал на другой инструмент, он становится активным. Затем новый режим передаётся в InputController."),
    p("После этого syncPlacementPanelState проходит по всем карточкам и выставляет им флаги checked или unchecked. Благодаря этому активная карточка подсвечивается."),

    p("8. Как режим панели попадает в игровую логику", 2),
    p("Класс HexSphereWidget не создаёт объекты на сфере напрямую. Он только меняет выбранный режим в InputController. Это важный архитектурный момент: UI не работает с данными сцены сам, а сообщает контроллеру, что теперь активен, например, режим Factory."),
    p("Метод InputController::setPlacementModel сохраняет новый режим в поле placementModel_. Одновременно он формирует текстовую подсказку для пользователя. Например, для Factory показывается сообщение, что режим постановки фабрики включён, а для Delete — что можно кликнуть по фабрике или руднику и удалить их."),

    p("9. Как работает клик по сфере", 2),
    p("Центральная точка входа — метод InputController::mousePress. Он вызывается каждый раз, когда пользователь нажимает кнопку мыши в окне."),
    p("Если нажата правая кнопка мыши, начинается вращение камеры. Это отдельный режим и к панели не относится."),
    p("Если нажата левая кнопка мыши, контроллер сначала определяет, куда именно попал клик. Для этого вызывается pickSceneAt."),
    p("pickSceneAt объединяет два вида проверки. Сначала проверяется попадание в сущности через pickEntityAt. Потом проверяется попадание в поверхность планеты через pickTerrainAt. Если попадание есть и туда и туда, выбирается более близкий по расстоянию результат."),
    p("Результат хранится в структуре PickHit. В ней есть cellId, entityId, координата точки попадания, расстояние t и флаг isEntity, который говорит, кликнули ли мы в объект или в саму поверхность."),

    p("10. Как происходит постановка фабрики или рудника", 2),
    p("Если активен режим Factory или Mine, в mousePress срабатывает ветка isPlacementModeActive. Если клик попал в уже существующий объект, контроллер пишет сообщение, что выбранная клетка занята. Если клик попал в свободную клетку поверхности, вызывается placeBuildingOnCell."),
    p("В начале placeBuildingOnCell выполняется проверка корректности cellId. Затем вызывается isCellOccupied. Этот метод проходит по всем сущностям ECS и смотрит, есть ли уже объект с таким currentCell. Если да, создание запрещается."),
    p("Если клетка свободна, создаётся новая сущность через ecs_.createEntity. Имя сущности выбирается по placementModelName. После этого в ECS добавляется компонент Mesh с meshId равным factory или mine."),
    p("Затем добавляется компонент Transform. В него записывается позиция, вычисленная специальной функцией проекта для поиска координаты на поверхности сферы по номеру выбранной ячейки. То есть объект ставится точно в нужную клетку планеты."),
    p("После этого добавляется компонент Collider с радиусом 0.16. Он нужен, чтобы потом по объекту можно было кликать и чтобы система pickEntityAt могла определить попадание луча в объект."),
    p("В конце пользователю показывается сообщение вида Factory placed on cell N или Mine placed on cell N."),

    p("11. Почему после постановки объект нельзя двигать", 2),
    p("Запрет на перемещение сделан логически, а не через блокировку интерфейса. В проекте уже существовала функция isMovableEntity. Она проверяет компонент Mesh у сущности."),
    p("Если meshId равен factory или mine, функция возвращает false. Это означает, что фабрика и рудник считаются статичными строениями."),
    p("Когда пользователь пытается переместить выбранную сущность через уже существующую механику moveSelectedEntityToCell, в начале метода вызывается isMovableEntity. Если объект статичный, перемещение сразу отменяется и выводится сообщение Static building cannot be moved."),
    p("То есть запрет на перемещение интегрирован в старую систему перемещения и не требует отдельной логики специально для панели."),

    p("12. Как работает удаление через карточку Delete", 2),
    p("Delete реализована не как отдельная кнопка вне системы, а как такой же PlacementModel, как Factory и Mine. Это удобно, потому что UI работает по одной и той же схеме: пользователь выбирает инструмент, затем кликает по сцене."),
    p("Если активен режим Delete, mousePress не вызывает placeBuildingOnCell, а вызывает deleteEntityAtHit."),
    p("Метод deleteEntityAtHit анализирует объект PickHit. Если клик попал прямо в сущность, берётся entityId из hit. Если клик попал в клетку поверхности, дополнительно выполняется поиск сущности, которая стоит на этой клетке."),
    p("Дальше вызывается isDeletableEntity. Этот метод проверяет meshId сущности и разрешает удалять только factory и mine. Машинка с meshId car не удаляется этим режимом."),
    p("Если найден допустимый объект, он удаляется из ECS. Если этот объект был выделен, выделение сначала снимается. После этого пользователю показывается сообщение о том, что строение удалено."),

    p("13. Как определяется попадание мыши в объект или ячейку", 2),
    p("Это важная часть объяснения преподавателю, потому что именно она связывает 2D-клик мыши с 3D-миром."),
    p("Для поверхности планеты используется pickTerrainAt. Сначала из камеры берутся начало луча rayOrigin и направление rayDirectionFromScreen. Потом этот луч проверяется на пересечение со всеми треугольниками terrain mesh. Для проверки пересечения используется алгоритм Мёллера — Трумбора."),
    p("Для сущностей используется pickEntityAt. Здесь каждый объект рассматривается как сфера коллизии с центром в transform.position и радиусом collider.radius. Луч пересекается с этой сферой, и если пересечение существует, объект считается выбранным."),
    p("После этого pickSceneAt сравнивает оба результата и выбирает ближайший к камере. Поэтому если пользователь кликает по модели, а не по земле за ней, система обычно выбирает именно модель."),

    p("14. Как новые объекты потом отображаются в рендерере", 2),
    p("Панель сама ничего не рисует в 3D. Она только создаёт сущности в ECS и задаёт им meshId."),
    p("Когда происходит очередной кадр, InputController::render передаёт в HexSphereRenderer граф сцены, в котором уже есть ECS с новыми сущностями."),
    p("Дальше EntityRenderer проходит по сущностям с компонентами Mesh и Transform. Если meshId равно factory, вызывается renderFactory. Если meshId равно mine, вызывается renderMine. Таким образом уже существующая 3D-логика автоматически начинает рисовать новые поставленные пользователем объекты."),

    p("15. Почему решение удобно с архитектурной точки зрения", 2),
    p("Плюс решения в том, что UI и игровая логика разделены. HexSphereWidget отвечает за создание кнопок, анимацию панели и визуальную обратную связь. InputController отвечает за поведение сцены и работу с данными."),
    p("Это значит, что в будущем можно добавить новые карточки по той же схеме. Например, можно добавить карточку Warehouse или карточку Road, просто расширив PlacementModel, добавив ещё одну кнопку в панели и прописав новую логику в контроллере."),
    p("Также решение хорошо тем, что не ломает старую архитектуру. Используются уже существующие ECS, уже существующий pick по сцене и уже существующий рендерер. Новая панель просто подключается к этим системам."),

    p("16. Короткий алгоритм для устного объяснения преподавателю", 2),
    p("Шаг 1. Я добавил в HexSphereWidget визуальную панель поверх QOpenGLWidget, потому что этот класс уже отвечает за окно сцены и умеет содержать обычные Qt-элементы."),
    p("Шаг 2. В панели я создал карточки на QToolButton: Factory, Mine и Delete. Для Factory и Mine я загрузил превью через QPixmap из resources/models/factory.jpg и resources/models/mine.jpg."),
    p("Шаг 3. Я добавил в InputController перечисление PlacementModel, чтобы хранить текущий выбранный инструмент."),
    p("Шаг 4. При нажатии на карточку панель вызывает setPlacementModel в контроллере, то есть UI только сообщает, какой режим теперь активен."),
    p("Шаг 5. При клике мышью по сцене контроллер делает pickSceneAt и получает, куда именно пользователь нажал: в объект или в ячейку поверхности."),
    p("Шаг 6. Если активен Factory или Mine, контроллер проверяет, свободна ли ячейка, и создаёт сущность в ECS с нужным meshId и Transform на поверхности сферы."),
    p("Шаг 7. Если активен Delete, контроллер ищет фабрику или рудник под курсором и удаляет сущность через ECS."),
    p("Шаг 8. Отрисовка новых объектов происходит автоматически через уже существующий EntityRenderer, потому что он и так умеет рисовать сущности с meshId factory и mine."),

    p("17. Итог", 2),
    p("В результате была добавлена полноценная пользовательская панель для работы с объектами на планете. Пользователь может открыть панель, выбрать тип действия, кликнуть по ячейке сферы и получить ожидаемое поведение: установка фабрики, установка рудника или удаление здания. Внутри проекта это реализовано через связку Qt UI, InputController, ECS и существующий 3D-рендерер."),
]


document_xml = f"""<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<w:document xmlns:wpc="http://schemas.microsoft.com/office/word/2010/wordprocessingCanvas"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:o="urn:schemas-microsoft-com:office:office"
 xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"
 xmlns:m="http://schemas.openxmlformats.org/officeDocument/2006/math"
 xmlns:v="urn:schemas-microsoft-com:vml"
 xmlns:wp14="http://schemas.microsoft.com/office/word/2010/wordprocessingDrawing"
 xmlns:wp="http://schemas.openxmlformats.org/drawingml/2006/wordprocessingDrawing"
 xmlns:w10="urn:schemas-microsoft-com:office:word"
 xmlns:w="http://schemas.openxmlformats.org/wordprocessingml/2006/main"
 xmlns:w14="http://schemas.microsoft.com/office/word/2010/wordml"
 xmlns:wpg="http://schemas.microsoft.com/office/word/2010/wordprocessingGroup"
 xmlns:wpi="http://schemas.microsoft.com/office/word/2010/wordprocessingInk"
 xmlns:wne="http://schemas.microsoft.com/office/word/2006/wordml"
 xmlns:wps="http://schemas.microsoft.com/office/word/2010/wordprocessingShape"
 mc:Ignorable="w14 wp14">
  <w:body>
    {' '.join(paragraphs)}
    <w:sectPr>
      <w:pgSz w:w="11906" w:h="16838"/>
      <w:pgMar w:top="1134" w:right="1134" w:bottom="1134" w:left="1134" w:header="708" w:footer="708" w:gutter="0"/>
    </w:sectPr>
  </w:body>
</w:document>
"""

content_types = """<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types">
  <Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/>
  <Default Extension="xml" ContentType="application/xml"/>
  <Override PartName="/word/document.xml" ContentType="application/vnd.openxmlformats-officedocument.wordprocessingml.document.main+xml"/>
</Types>
"""

rels = """<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">
  <Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="word/document.xml"/>
</Relationships>
"""

OUT.parent.mkdir(parents=True, exist_ok=True)
if OUT.exists():
    OUT.unlink()

with ZipFile(OUT, "w", ZIP_DEFLATED) as zf:
    zf.writestr("[Content_Types].xml", content_types.encode("utf-8"))
    zf.writestr("_rels/.rels", rels.encode("utf-8"))
    zf.writestr("word/document.xml", document_xml.encode("utf-8"))

print(OUT)
